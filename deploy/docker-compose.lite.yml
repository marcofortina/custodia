# Copyright (c) 2026 Marco Fortina
# SPDX-License-Identifier: AGPL-3.0-only
#
# This file is part of Custodia.
# Custodia is distributed under the GNU Affero General Public License v3.0.
# See the accompanying LICENSE file for details.

services:
  custodia-lite:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
      args:
        GO_BUILD_TAGS: sqlite
        CUSTODIA_VERSION: "${CUSTODIA_VERSION:-lite-dev}"
        CUSTODIA_COMMIT: "${CUSTODIA_COMMIT:-unknown}"
        CUSTODIA_DATE: "${CUSTODIA_DATE:-unknown}"
    command: ["/usr/local/bin/custodia-server", "--config", "/etc/custodia/custodia-server.yaml"]
    environment:
      CUSTODIA_PROFILE: lite
    volumes:
      - ./examples/custodia-server.lite.yaml:/etc/custodia/custodia-server.yaml:ro
      - ../.dev-certs:/etc/custodia:ro
      - custodia-lite-data:/var/lib/custodia
      - custodia-lite-logs:/var/log/custodia
    ports:
      - "8443:8443"
      - "9443:9443"
      - "8080:8080"
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://127.0.0.1:8080/live"]
      interval: 10s
      timeout: 3s
      retries: 3

volumes:
  custodia-lite-data:
  custodia-lite-logs:

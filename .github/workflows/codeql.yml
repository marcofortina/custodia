# Copyright (c) 2026 Marco Fortina
# SPDX-License-Identifier: AGPL-3.0-only
#
# This file is part of Custodia.
# Custodia is distributed under the GNU Affero General Public License v3.0.
# See the accompanying LICENSE file for details.

name: CodeQL

on:
  push:
    branches: [main, master]
  pull_request:
  schedule:
    - cron: '37 3 * * 1'

permissions:
  contents: read
  security-events: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  CODEQL_EXTRACTOR_GO_OPTION_EXTRACT_TESTS: 'true'

jobs:
  analyze:
    name: Analyze ${{ matrix.language }}
    runs-on: ubuntu-latest
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: go
            build: go
            build_mode: manual
          - language: python
            build: none
            build_mode: none
          - language: javascript-typescript
            build: none
            build_mode: none
          - language: java-kotlin
            build: java
            build_mode: manual
          - language: c-cpp
            build: none
            build_mode: none
          - language: rust
            build: none
            build_mode: none

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        if: ${{ matrix.build == 'go' }}
        with:
          go-version: '1.26.x'
          cache: true
          cache-dependency-path: go.mod

      - uses: actions/setup-python@v6
        if: ${{ matrix.language == 'python' }}
        with:
          python-version: '3.12'

      - uses: actions/setup-node@v6
        if: ${{ matrix.language == 'javascript-typescript' }}
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: clients/node/package.json

      - name: Install compiled-language dependencies
        if: ${{ matrix.build_mode == 'manual' && matrix.build == 'java' }}
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            default-jdk-headless \
            g++ \
            libcurl4-openssl-dev \
            libssl-dev \
            pkg-config

      - name: Install Rust toolchain
        if: ${{ matrix.build_mode == 'manual' && matrix.build == 'rust' }}
        run: |
          rustup toolchain install stable --profile minimal
          rustup default stable

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build_mode }}
          queries: security-extended,security-and-quality

      - name: Build Go extraction target
        if: ${{ matrix.build == 'go' }}
        run: |
          go build ./...
          go test -run '^$' ./...
          go build -tags sqlite ./...
          go test -run '^$' -tags sqlite ./...
          go build -tags postgres ./...
          go test -run '^$' -tags postgres ./...

      - name: Build Java extraction target
        if: ${{ matrix.build == 'java' }}
        run: make test-java-client

      - name: Build C++ extraction target
        if: ${{ matrix.build == 'cpp' }}
        run: make test-cpp-client

      - name: Build Rust extraction target
        if: ${{ matrix.build_mode == 'manual' && matrix.build == 'rust' }}
        run: make test-rust-client

      - name: Analyze
        uses: github/codeql-action/analyze@v4
        with:
          category: /language:${{ matrix.language }}
